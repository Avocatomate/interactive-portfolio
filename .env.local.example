# ── AI Provider ───────────────────────────────────────────
# Option A: OpenAI (recommended, gpt-4o-mini is fast + cheap)
OPENAI_API_KEY=your_openai_api_key_here

# Option B: Groq (faster free tier — swap model in app/api/chat/route.ts)
# GROQ_API_KEY=your_groq_api_key_here
